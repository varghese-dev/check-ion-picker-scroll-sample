import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'check-ion-picker-scroll-sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
